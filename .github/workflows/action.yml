name: My Workflow
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@dev
    - name: Run action
      # Put your action repo here
      uses: me/test_calculette.py@dev

      # Put an example of your mandatory inputs here
      with:
        myInput: world
